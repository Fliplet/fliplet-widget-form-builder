<template v-for="(option, index) in options">
  <div class="radio radio-icon">
    <input type="radio" :id="_elementId + '-' + index" :name="name" v-model="value" :required="required" :value="option.id" v-on:change="updateValue()" >
    <label :for="_elementId + '-' + index">
      <span class="check"><i class="fa fa-circle"></i></span> \{{ option.label || option.id }}
    </label>
  </div>
</template>
