name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm i
    - name: Install ESLint
      run: npm i eslint -g
    - name: Run ESLint
      run: eslint . --ext .js,.jsx,.ts,.tsx